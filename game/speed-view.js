export function speedScene(q,escape){
 const e=escape||String,s=q.scene;
 const round=n=>`<svg viewBox="0 0 120 120" role="img" aria-label="Maximumsnelheidsbord ${n}"><circle cx="60" cy="60" r="55" fill="white" stroke="#e34c55" stroke-width="10"/><text x="60" y="78" text-anchor="middle" fill="#172335" font-family="Arial,sans-serif" font-weight="700" font-size="${n>99?44:52}">${n}</text></svg>`;
 return `<figure class="speed-scene"><div class="speed-scene-head"><span>${e(s.road)}</span>${s.time?`<strong class="scene-clock">◷ ${e(s.time)}</strong>`:''}</div><div class="road-illustration" aria-hidden="true"><div class="road-dash"></div><div class="hero-car"></div></div><div class="speed-evidence">${s.limit?`<div class="road-sign">${s.zone?'<b class="zone-label">ZONE</b>':''}${round(s.limit)}${s.window?`<div class="time-plate">${e(s.window)}</div>`:''}<small>Vast bord</small></div>`:''}${s.matrix?`<div class="matrix-holder"><div class="matrix-panel" role="img" aria-label="Matrixbord ${s.matrix} km per uur"><span>${s.matrix}</span></div><small>Matrix boven jouw rijstrook</small></div>`:''}${s.sign?`<div class="road-sign"><img src="./assets/signs/${s.sign}.png" alt="Verkeersbord ${s.sign}"><small>Bord ${s.sign}</small></div>`:''}${s.advisory?`<div class="road-sign"><div class="advisory-sign" role="img" aria-label="Blauw vierkant adviesbord ${s.advisory}">${s.advisory}</div><small>Adviesbord A4</small></div>`:''}${!s.limit&&!s.matrix&&!s.sign&&!s.advisory?'<div class="scene-note">Lees het wegtype<br>en de voorwaarden.</div>':''}</div><figcaption>Personenauto zonder aanhangwagen, tenzij de vraag anders vermeldt. De tekening is schematisch; alle voorwaarden staan in de vraag.</figcaption></figure>`;
}
